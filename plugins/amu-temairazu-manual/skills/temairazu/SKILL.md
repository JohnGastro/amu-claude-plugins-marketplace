---
name: temairazu
description: "TEMAIRAZU（手間いらず）の操作方法をマニュアルから調べる。"
argument-hint: "<知りたい操作>"
allowed-tools: [Read, Glob, Grep]
---

# /temairazu

ユーザーの引数を、TEMAIRAZU（手間いらず）操作マニュアルへの質問として扱う。

`../temairazu-manual/SKILL.md` の方針に従い、`../temairazu-manual/references/` の目次・ページ別Markdown・全文Markdownから該当箇所を探して回答する。

回答には、可能な範囲で参照したページ名を含める。
